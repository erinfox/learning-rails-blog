<h1>Welcome#index</h1>

<ul>
  <% @users.each do |user| %>
  <li><a href="/users/<%= user.id %>"><%= user.id %></a></li>
  <li><%= link_to user.id, "/users/#{user.id}" %></li>
  <li><%= link_to user.id, user_path(user.id) %></li>

  <% end %>
</ul>
